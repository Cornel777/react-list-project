import styled from "styled-components";

const NameControlButtonsContainer = styled.div`
  display: flex;
  justify-content: space-evenly;
  margin: 1rem;

  button {
    margin: 0 0.6rem;
  }
`;

export default NameControlButtonsContainer;